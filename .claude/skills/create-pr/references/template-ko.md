<!--
이 파일은 한국 프로젝트(팀 내부 저장소, 국내 오픈소스 등)용 PR 본문 템플릿이다.
fork 서브에이전트는 아래 구조를 그대로 채워 PR 본문을 작성한다.
각 섹션 아래 괄호 설명은 무엇을 써야 하는지 알려주는 가이드이므로, 실제 PR 본문에는 포함하지 않는다.
diff·커밋 로그에서 확인할 수 없는 내용은 지어내지 말고 "해당 없음"으로 남긴다.
-->

## 요약

(이 PR이 왜 필요한지, 무엇을 해결하는지 2~3문장으로. "무엇을 바꿨는지"는 diff로 보이므로 "왜"에 집중한다.)

## 변경 사항

- (커밋 단위로 묶어서 핵심 변경을 불릿으로 나열. 파일 나열이 아니라 동작·구조 변화 중심으로 작성)

## 테스트 계획

- [ ] (실행한 lint/test/build 명령과 결과를 체크리스트로. best-effort 검증 결과를 그대로 반영)
- [ ] (수동으로 확인한 경우 확인 절차와 결과)

## 관련 이슈

(연결된 이슈 번호가 있으면 `Closes #123` 형식으로. 없으면 "해당 없음")

## 스크린샷 (선택)

(UI 변경이 있고 캡처 가능한 경우에만 포함. 아니면 이 섹션 자체를 생략)
