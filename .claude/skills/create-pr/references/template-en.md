<!--
This is the PR body template for foreign/international open-source projects.
The fork subagent fills in this structure exactly to write the PR body.
The parenthetical notes under each heading are guidance for the agent on what to write —
do not include them in the actual PR body.
Never fabricate content the diff/commit log doesn't support; leave "N/A" instead.
-->

## Summary

(2-3 sentences on why this PR exists and what problem it solves. The diff already shows
"what changed" — focus on "why".)

## Changes

- (Bullet list grouped by commit, focused on behavioral/structural changes rather than a file listing)

## Test plan

- [ ] (Lint/test/build commands actually run, with their results — reflect the best-effort
      verification output as-is)
- [ ] (Manual verification steps and outcome, if applicable)

## Related issue

(Use `Closes #123` if there's a linked issue, otherwise "N/A")

## Screenshots (optional)

(Only include if there's a UI change and a capture is available; otherwise omit this section entirely)
